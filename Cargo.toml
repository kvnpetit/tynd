[workspace]
members  = ["packages/host-rs", "packages/full", "packages/lite"]
resolver = "2"

[profile.release]
opt-level     = 3
lto           = "fat"
codegen-units = 1
panic         = "abort"
strip         = true
debug         = false
incremental   = false
