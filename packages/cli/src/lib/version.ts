/** Single source of truth for the CLI version. Keep in sync with package.json. */
export const VERSION = "0.1.0"

/** GitHub repo (owner/name) used for release downloads. */
export const REPO = "kvnpetit/tynd"
