{
  "name": "vorn",
  "private": true,
  "workspaces": ["packages/*", "playground/*", "benchmarks/*"],
  "scripts": {
    "bench": "bun benchmarks/run.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vorn/cli":  "workspace:*",
    "@vorn/core": "workspace:*"
  }
}
