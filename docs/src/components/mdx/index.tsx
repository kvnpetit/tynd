// Custom MDX components shared across doc pages. Register new entries in
// `src/mdx-components.tsx`.
//
// Add helpers as the docs grow: VersionBadge, PlatformIcon, RuntimeBadge,
// CliOutput, CompareTable, etc.

export {};
